rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /results/{resultId} {
      allow read, write: if
          request.auth != null || request.auth.uid == 'aptitude-box-service-worker';
    }
  }
}